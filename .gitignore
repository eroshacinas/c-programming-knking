# Ignore all object files anywhere
**/*.o

# Ignore executables with no extension in any folder
# This uses a glob that matches files without dots in their names
**/[!.]*       # ignore files without leading dot or extension
**/[!.]*/*     # also catch executables inside nested folders

# Allow source and header files everywhere
!**/*.c
!**/*.h

# Explicitly ignore common GCC outputs
**/a.out
**/*.out

# Optional: ignore debug symbols, core dumps, etc.
**/core
**/*.dSYM/